{
  "name": "xethya",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "^10.16.3",
    "yarn": "^1.17.3"
  },
  "scripts": {
    "build": "lerna run build --sort",
    "test": "lerna run test",
    "prepublish": "lerna run build --sort",
    "package:create": "node tools/package-create"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.2",
    "jest": "^24.9.0",
    "lerna": "^3.4.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.11.0",
    "typescript": "^3.1.1"
  }
}
